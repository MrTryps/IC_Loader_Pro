<UserControl x:Class="IC_Loader_Pro.Dockpane_IC_LoaderView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:extensions="clr-namespace:ArcGIS.Desktop.Extensions;assembly=ArcGIS.Desktop.Extensions"
             mc:Ignorable="d" 
             d:DesignHeight="700" d:DesignWidth="800"
             d:DataContext="{Binding Path=ui.Dockpane_IC_LoaderViewModel}"
             Background="{DynamicResource Esri_BackgroundBrush}">

    <UserControl.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <extensions:DesignOnlyResourceDictionary Source="pack://application:,,,/ArcGIS.Desktop.Framework;component\Themes\Default.xaml"/>
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </UserControl.Resources>

    <Grid>
        <DockPanel Margin="5">
            <TextBlock DockPanel.Dock="Top" Text="IC Queues" FontWeight="Bold"/>
            <ListBox DockPanel.Dock="Top" ItemsSource="{Binding ICQueues}" SelectedItem="{Binding SelectedQueue}">
                <ListBox.ItemsPanel>
                    <ItemsPanelTemplate>
                        <StackPanel Orientation="Horizontal"/>
                    </ItemsPanelTemplate>
                </ListBox.ItemsPanel>
                <ListBox.ItemTemplate>
                    <DataTemplate>
                        <ToggleButton Style="{DynamicResource Esri_ButtonToggle}" Margin="2,5"
                                      IsChecked="{Binding Path=IsSelected, RelativeSource={RelativeSource AncestorType=ListBoxItem}}">
                            <StackPanel Orientation="Horizontal">
                                <TextBlock Text="{Binding Name}"/>
                                <TextBlock Text="{Binding EmailCount, StringFormat=' ({0})'}"/>
                            </StackPanel>
                        </ToggleButton>
                    </DataTemplate>
                </ListBox.ItemTemplate>
            </ListBox>

            <TextBlock Text="Selected Queue Details" DockPanel.Dock="Top" Margin="0,10,0,0" FontWeight="Bold"/>
            <StackPanel DockPanel.Dock="Top">
                <TextBlock Text="{Binding SelectedQueue.Name, StringFormat='Name: {0}', FallbackValue='Name: (nothing selected)'}"/>
                <TextBlock Text="{Binding SelectedQueue.EmailCount, StringFormat='Emails: {0}', FallbackValue='Emails: (nothing selected)'}"/>
            </StackPanel>
        </DockPanel>
    </Grid>
</UserControl>