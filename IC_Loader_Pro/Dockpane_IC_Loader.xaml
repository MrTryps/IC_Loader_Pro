<UserControl x:Class="IC_Loader_Pro.Dockpane_IC_LoaderView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:extensions="clr-namespace:ArcGIS.Desktop.Extensions;assembly=ArcGIS.Desktop.Extensions"
             mc:Ignorable="d" 
             d:DesignHeight="700" d:DesignWidth="800"
             d:DataContext="{Binding Path=ui.Dockpane_IC_LoaderViewModel}"
             Background="{DynamicResource Esri_BackgroundBrush}">

    <UserControl.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <extensions:DesignOnlyResourceDictionary Source="pack://application:,,,/ArcGIS.Desktop.Framework;component\Themes\Default.xaml"/>
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </UserControl.Resources>

    <Grid Margin="10" IsEnabled="{Binding IsUIEnabled}">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/> <RowDefinition Height="Auto"/> <RowDefinition Height="Auto"/> <RowDefinition Height="Auto"/> <RowDefinition Height="*"/>    <RowDefinition Height="Auto"/> </Grid.RowDefinitions>

        <Grid Grid.Row="0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            <Menu Grid.Column="0" VerticalAlignment="Center" Background="Transparent">
                <MenuItem Header="_Notes" Command="{Binding ShowNotesCommand}"/>
                <MenuItem Header="_Search" Command="{Binding SearchCommand}"/>
                <MenuItem Header="_Tools" Command="{Binding ToolsCommand}"/>
                <MenuItem Header="_Options" Command="{Binding OptionsCommand}"/>
            </Menu>
            <ListBox Grid.Column="1" ItemsSource="{Binding _readOnlyListOfQueues}" SelectedItem="{Binding SelectedIcType}" BorderThickness="0" Background="Transparent">
                <ListBox.ItemsPanel><ItemsPanelTemplate><StackPanel Orientation="Horizontal"/></ItemsPanelTemplate></ListBox.ItemsPanel>
                <ListBox.ItemTemplate>
                    <DataTemplate>
                        <ToggleButton Style="{DynamicResource Esri_ButtonToggle}" Margin="2,0" IsChecked="{Binding Path=IsSelected, RelativeSource={RelativeSource AncestorType=ListBoxItem}, Mode=TwoWay}">
                            <StackPanel Orientation="Horizontal">
                                <TextBlock Text="{Binding Name}"/>
                                <TextBlock Text="{Binding EmailCount, StringFormat=' ({0})'}" Foreground="{DynamicResource Esri_Gray130Brush}"/>
                            </StackPanel>
                        </ToggleButton>
                    </DataTemplate>
                </ListBox.ItemTemplate>
            </ListBox>
        </Grid>

        <Separator Grid.Row="1" Margin="0,5"/>

        <Grid Grid.Row="2">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            <TextBlock Grid.Column="0" Text="{Binding StatusMessage, FallbackValue='Please select an IC Type to begin.'}" VerticalAlignment="Center" FontWeight="Bold" TextTrimming="CharacterEllipsis"/>
            <StackPanel Grid.Column="1" Orientation="Horizontal" VerticalAlignment="Center">
                <TextBlock Text="Passed:" Margin="15,0,5,0" Foreground="{DynamicResource Esri_Gray130Brush}"/>
                <TextBlock Text="{Binding SelectedQueue.PassedCount, FallbackValue='0'}" Margin="0,0,15,0" FontWeight="SemiBold"/>
                <TextBlock Text="Skipped:" Margin="0,0,5,0" Foreground="{DynamicResource Esri_Gray130Brush}"/>
                <TextBlock Text="{Binding SelectedQueue.SkippedCount, FallbackValue='0'}" Margin="0,0,15,0" FontWeight="SemiBold"/>
                <TextBlock Text="Failed:" Margin="0,0,5,0" Foreground="{DynamicResource Esri_Gray130Brush}"/>
                <TextBlock Text="{Binding SelectedQueue.FailedCount, FallbackValue='0'}" FontWeight="SemiBold"/>
            </StackPanel>
        </Grid>

        <Separator Grid.Row="3" Margin="0,5"/>

        <Grid Grid.Row="4">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            <GroupBox Grid.Column="0" Header="Shapes to Review" Margin="0,0,2,0">
                <DockPanel>
                    <StackPanel DockPanel.Dock="Top" Orientation="Horizontal" Margin="5">
                        <CheckBox Content="Show Valid" IsChecked="{Binding ShowValid}" Margin="0,0,10,0"/>
                        <CheckBox Content="Show Invalid" IsChecked="{Binding ShowInvalid}" Margin="0,0,10,0"/>
                    </StackPanel>
                    <ListView ItemsSource="{Binding ShapesToReview}" SelectedItem="{Binding SelectedShapeForReview}"/>
                </DockPanel>
            </GroupBox>
            <GridSplitter Grid.Column="1" Width="3" HorizontalAlignment="Stretch" Background="Transparent"/>
            <GroupBox Grid.Column="2" Header="Selected Shapes to Use" Margin="2,0,0,0">
                <ListView ItemsSource="{Binding SelectedShapes}" SelectedItem="{Binding SelectedShapeToUse}"/>
            </GroupBox>
        </Grid>

        <StackPanel Grid.Row="5" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,10,0,0">
            <Button Content="Save" Command="{Binding SaveCommand}" Style="{DynamicResource Esri_ButtonSuccess}" Margin="5,0" Width="80"/>
            <Button Content="Skip" Command="{Binding SkipCommand}" Margin="5,0" Width="80"/>
            <Button Content="Reject" Command="{Binding RejectCommand}" Style="{DynamicResource Esri_ButtonDanger}" Margin="5,0" Width="80"/>
        </StackPanel>
    </Grid>
</UserControl>