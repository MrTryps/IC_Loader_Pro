<Window x:Class="IC_Loader_Pro.Views.CreateIcDeliverableWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
        mc:Ignorable="d"
        Title="Create New IC Deliverable" Height="320" Width="500"
        WindowStartupLocation="CenterOwner" ResizeMode="NoResize"
        
        xmlns:viewModels="clr-namespace:IC_Loader_Pro.ViewModels"
        
        d:DataContext="{d:DesignInstance Type=viewModels:CreateIcDeliverableViewModel, IsDesignTimeCreatable=False}">

    <Window.Resources>
        <Style TargetType="TextBlock" x:Key="ValidationTextBlockStyle">
            <Setter Property="Margin" Value="0,2,0,10"/>
            <Setter Property="FontStyle" Value="Italic"/>
            <Style.Triggers>
                <DataTrigger Binding="{Binding IsPrefIdValid}" Value="True">
                    <Setter Property="Foreground" Value="Green"/>
                </DataTrigger>
                <DataTrigger Binding="{Binding IsPrefIdValid}" Value="False">
                    <Setter Property="Foreground" Value="OrangeRed"/>
                </DataTrigger>
            </Style.Triggers>
        </Style>
    </Window.Resources>

    <Grid Margin="15">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="Auto"/>
        </Grid.ColumnDefinitions>

        <TextBlock Grid.Row="0" Grid.Column="0" Text="IC Type:" VerticalAlignment="Center" Margin="0,0,10,0"/>
        <ComboBox Grid.Row="0" Grid.Column="1" Grid.ColumnSpan="2" ItemsSource="{Binding IcTypes}" SelectedItem="{Binding SelectedIcType}"/>

        <TextBlock Grid.Row="1" Grid.Column="0" Text="Pref ID:" VerticalAlignment="Center" Margin="0,15,10,0"/>
        <TextBox Grid.Row="1" Grid.Column="1" Text="{Binding PrefId, UpdateSourceTrigger=PropertyChanged}" VerticalAlignment="Center" Margin="0,15,0,0"/>
        <Button Grid.Row="1" Grid.Column="2" Content="Validate" Command="{Binding ValidatePrefIdCommand}" Width="80" Margin="5,15,0,0"/>
        <TextBlock Grid.Row="2" Grid.Column="1" Grid.ColumnSpan="2" Text="{Binding ValidationStatus}" Style="{StaticResource ValidationTextBlockStyle}"/>

        <TextBlock Grid.Row="3" Grid.Column="0" Text="GIS File:" VerticalAlignment="Center" Margin="0,0,10,0"/>
        <TextBox Grid.Row="3" Grid.Column="1" Text="{Binding GisFilePath}" IsReadOnly="True" VerticalAlignment="Center"/>
        <Button Grid.Row="3" Grid.Column="2" Content="Browse..." Command="{Binding BrowseForFileCommand}" Width="80" Margin="5,0,0,0"/>

        <StackPanel Grid.Row="7" Grid.Column="1" Grid.ColumnSpan="2" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button Content="Create" IsDefault="True" Command="{Binding CreateDeliverableCommand}" Click="CreateButton_Click" Width="80" Margin="0,0,10,0"/>
            <Button Content="Cancel" IsCancel="True" Width="80"/>
        </StackPanel>
    </Grid>
</Window>