<Window x:Class="IC_Loader_Pro.Views.ConnectionTesterWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
        mc:Ignorable="d"
        Title="Geodatabase Connection Tester" Height="320" Width="500"
        WindowStartupLocation="CenterOwner" ResizeMode="NoResize"
        xmlns:viewModels="clr-namespace:IC_Loader_Pro.ViewModels"
        d:DataContext="{d:DesignInstance Type=viewModels:ConnectionTesterViewModel, IsDesignTimeCreatable=True}">

    <Window.Resources>
        <Style TargetType="TextBlock" x:Key="StatusTextBlockStyle">
            <Setter Property="Margin" Value="0,15,0,0"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Style.Triggers>
                <DataTrigger Binding="{Binding IsSuccess}" Value="True">
                    <Setter Property="Foreground" Value="Green"/>
                </DataTrigger>
                <DataTrigger Binding="{Binding IsSuccess}" Value="False">
                    <Setter Property="Foreground" Value="Red"/>
                </DataTrigger>
            </Style.Triggers>
        </Style>
    </Window.Resources>

    <Grid Margin="15">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <TextBlock Grid.Row="0" Grid.Column="0" Text="Instance:" VerticalAlignment="Center" Margin="0,0,10,10"/>
        <TextBox Grid.Row="0" Grid.Column="1" Text="{Binding Instance}" VerticalAlignment="Center" Margin="0,0,0,10"/>

        <TextBlock Grid.Row="1" Grid.Column="0" Text="Database:" VerticalAlignment="Center" Margin="0,0,10,10"/>
        <TextBox Grid.Row="1" Grid.Column="1" Text="{Binding Database}" VerticalAlignment="Center" Margin="0,0,0,10"/>

        <TextBlock Grid.Row="2" Grid.Column="0" Text="User:" VerticalAlignment="Center" Margin="0,0,10,10"/>
        <TextBox Grid.Row="2" Grid.Column="1" Text="{Binding User, UpdateSourceTrigger=PropertyChanged}" VerticalAlignment="Center" Margin="0,0,0,10"/>

        <TextBlock Grid.Row="3" Grid.Column="0" Text="Password:" VerticalAlignment="Center" Margin="0,0,10,10"/>
        <PasswordBox Grid.Row="3" Grid.Column="1" x:Name="PasswordBox" VerticalAlignment="Center" Margin="0,0,0,10"/>

        <TextBlock Grid.Row="4" Grid.Column="0" Grid.ColumnSpan="2" Text="{Binding StatusMessage}" Style="{StaticResource StatusTextBlockStyle}"/>

        <StackPanel Grid.Row="5" Grid.Column="1" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button Content="Test Connection" Command="{Binding TestConnectionCommand}" Click="TestButton_Click" IsDefault="True" Width="120" Margin="0,0,10,0"/>
            <Button Content="Close" IsCancel="True" Width="80"/>
        </StackPanel>
    </Grid>
</Window>