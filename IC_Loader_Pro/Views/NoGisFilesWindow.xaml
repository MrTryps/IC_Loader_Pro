<Window x:Class="IC_Loader_Pro.Views.NoGisFilesWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="No GIS Files Found - Action Required" Height="250" Width="500"
        WindowStartupLocation="CenterOwner" ResizeMode="NoResize">
    <Grid Margin="15">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <TextBlock Grid.Row="0" Grid.ColumnSpan="2" Text="This email contains no valid GIS datasets. Please choose an action below." TextWrapping="Wrap" Margin="0,0,0,15"/>

        <TextBlock Grid.Row="1" Grid.Column="0" Text="Sender:" FontWeight="Bold" Margin="0,0,10,5"/>
        <TextBlock Grid.Row="1" Grid.Column="1" Text="{Binding Sender}" TextTrimming="CharacterEllipsis" ToolTip="{Binding Sender}"/>

        <TextBlock Grid.Row="2" Grid.Column="0" Text="Subject:" FontWeight="Bold" Margin="0,0,10,5"/>
        <TextBlock Grid.Row="2" Grid.Column="1" Text="{Binding Subject}" TextTrimming="CharacterEllipsis" ToolTip="{Binding Subject}"/>

        <Button Grid.Row="3" Grid.ColumnSpan="2" Content="View Original Email in Outlook" Command="{Binding ViewEmailCommand}" HorizontalAlignment="Center" Margin="0,10"/>

        <StackPanel Grid.Row="4" Grid.ColumnSpan="2" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,15,0,0">
            <Button Content="Treat as Correspondence" Width="160" IsDefault="True" Click="CorrespondenceButton_Click" Margin="0,0,10,0"/>
            <Button Content="Fail Deliverable" Width="120" IsCancel="False" Click="FailButton_Click"/>
        </StackPanel>
    </Grid>
</Window>